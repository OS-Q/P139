name: release

on:
  push:
    tags: 
      - 'v*'


jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    if: github.repository == 'OS-Q/P19'
    strategy:
      fail-fast: true
      matrix:
        python-version: [3.9]
        examples:
          - "examples/arduino-blink"
          - "examples/arduino-internal-libs"
          - "examples/native-blink"

    steps:
      - uses: actions/checkout@v2
        with:
          submodules: "recursive"
          fetch-depth: 1

      - name: Python ${{ matrix.python-version }}
        uses: actions/setup-python@v1
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: |
          sudo apt-get update
          sudo apt-get install -y sshpass
          python -m pip install --upgrade pip
          pip install -U https://github.com/OS-Q/S03/archive/master.zip
          qio platform install file://.

      - name: Build
        run: |
          qio run -d ${{ matrix.examples }}

  release:
    needs:
      - build
    if: success() && startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    steps:
      - name: Create Release # 创建Release，可以在仓库看到一个个版本
        id: create_release
        uses: actions/create-release@master
        env:
          GITHUB_TOKEN: ${{ secrets.OSQ }} 
        with:
          tag_name: ${{ github.ref }} # (tag)标签名称
          release_name: release ${{ github.ref }}
          draft: false # 是否是草稿
          prerelease: false # 是否是预发布

      - name: clone
        uses: actions/checkout@v2
        with:
          submodules: "recursive"
          fetch-depth: 1

      - name: package
        run: |
          zip -r P19.zip boards builder link.json

      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@master
        env:
          GITHUB_TOKEN: ${{ secrets.OSQ }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # 上传地址，通过创建Release获取到的
          asset_path: P19.zip 
          asset_name: P19${{ github.ref }}.zip # 上传后的文件名
          asset_content_type: application/x-zip

  deploy:
    needs:
      - release
    if: success() && needs.release.result != 'skipped'
    runs-on: ubuntu-latest

    steps:
      - name: download
        uses: appleboy/ssh-action@master 
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          password: ${{ secrets.DEPLOY_PASSWORD }}
          port: ${{ secrets.DEPLOY_SSHPORT }}
          script: |
            cd /home/www/html/package 
            wget https://github.com/OS-Q/P19/releases/latest/download/P19${{ github.ref }}.zip -O P19${{ github.ref }}.zip &
